# Groq API key (https://console.groq.com/keys). Without it the app runs rules-only and the analyst is disabled.
GROQ_API_KEY=
# Optional: override the model (must support tool calling)
GROQ_MODEL=openai/gpt-oss-120b
